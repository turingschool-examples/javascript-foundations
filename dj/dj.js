function createSong(name, artist, favorite) {
  return {
    name: name,
    artist: artist,
    favorite: favorite,
  }
}

module.exports = { 
  createSong, 
  // playSong, 
  // makePlaylist, 
  // addSongToPlaylist, 
  // playSongs
};
