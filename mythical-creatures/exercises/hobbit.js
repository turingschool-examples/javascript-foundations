
function createHobbit(name, age) {
  var hobbit = {
    name,
    age
  }
  return hobbit
}

module.exports = {
  createHobbit, 
  // celebrateBirthday, 
  // getRing, 
  // meetPeople, 
  // findFriends
}