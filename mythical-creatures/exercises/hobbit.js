function createHobbit(name = 'unknown', age = 0) {
  return{
    name: name,
    age: age,
    isAdult: 
  }
}

module.exports = {
  createHobbit, 
  // celebrateBirthday, 
  // getRing, 
  // meetPeople, 
  // findFriends
}