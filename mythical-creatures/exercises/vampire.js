function createVampire(name, pet = 'bat') {
  return vampire = {
    name: name,
    pet: pet
  }
}

module.exports = {
  createVampire, 
  // drink, 
  // findBatLovers, 
  // encounterDeliciousVictim, 
  // inquirePlace
}